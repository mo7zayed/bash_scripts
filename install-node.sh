# using nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/master/install.sh | bash &&
# source $HOME/.bashrc &&
# nvm list-remote // Will list available node versions
# install your version from the above command's results
# nvm install xxx
# nvm list